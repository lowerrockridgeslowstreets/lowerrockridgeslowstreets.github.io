# Copy to ../private/formspree-sync.env and fill in (private/ is gitignored — never commit real keys).
# Use the READ-ONLY API key from Formspree (not the master key).
# If these keys were ever shared or screenshotted, reset them in the Formspree dashboard first.

FORMSPREE_API_KEY=
FORMSPREE_FORM_HASHID=xjgagzdj
