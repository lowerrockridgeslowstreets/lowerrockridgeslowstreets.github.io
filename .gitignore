.DS_Store
.claude/
.cursor/
.env
# Telraam: absolute paths to local data (may contain home directory); never commit.
telraam_code/local_config.json
private/
__pycache__/
*.pyc

# Reference PDFs / notes — not part of the public Pages site (keep locally if needed)
reference-pdfs/
